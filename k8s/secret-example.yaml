apiVersion: v1
kind: Secret
metadata:
  name: approvalflow-secrets
  namespace: approvalflow
type: Opaque
stringData:
  # Local/demo placeholder only. Replace in a real cluster using a secret manager.
  payment-provider-token: replace-with-real-payment-provider-token

  # Optional. Only required when AGENT_PROVIDER=gemini.
  gemini-api-key: replace-with-real-gemini-api-key
