apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: approvalflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              value: approvalflow
            - name: POSTGRES_USER
              value: approvalflow
            - name: POSTGRES_PASSWORD
              value: approvalflow
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: approvalflow
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
